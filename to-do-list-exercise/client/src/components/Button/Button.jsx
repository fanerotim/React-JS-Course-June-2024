import './Button.css'

const Button = () => {
    return (
        <div className="add-btn-container">
            <button className="btn">+ Add new Todo</button>
        </div>
    )
}

export default Button;